[1mdiff --git a/app.py b/app.py[m
[1mindex 1a00976..7f2f283 100644[m
[1m--- a/app.py[m
[1m+++ b/app.py[m
[36m@@ -1,4 +1,5 @@[m
 import os[m
[32m+[m[32mimport tempfile[m
 [m
 import streamlit as st[m
 [m
[36m@@ -15,6 +16,7 @@[m [mfrom core.perfil_empresa import carregar_perfil, salvar_perfil[m
 from core.proposta_pdf import gerar_pdf_proposta[m
 from core.reporter import gerar_excel[m
 from core.validacao import validar_dados[m
[32m+[m[32mfrom config import LOCAL_OBRA[m
 from core.vision import ErroExtracaoAmigavel, extrair_dados_da_planta[m
 [m
 st.set_page_config(page_title="OrçaObra AI", page_icon="🏗️", layout="centered")[m
[36m@@ -155,11 +157,12 @@[m [mwith st.expander("💲 Personalizar Tabela de Preços (avançado)"):[m
         "📤 Enviar planilha de preços atualizada", type=["xlsx"], key="upload_tabela_precos"[m
     )[m
     if arquivo_precos is not None:[m
[31m-        caminho_temp_precos = os.path.join(PASTA_PERFIL, "temp_tabela_precos.xlsx")[m
[31m-        with open(caminho_temp_precos, "wb") as f:[m
[31m-            f.write(arquivo_precos.getbuffer())[m
[32m+[m[32m        with tempfile.NamedTemporaryFile(delete=False, suffix=".xlsx") as tmp:[m
[32m+[m[32m            tmp.write(arquivo_precos.getbuffer())[m
[32m+[m[32m            caminho_temp_precos = tmp.name[m
 [m
[31m-        atualizados, avisos = tabela_precos.importar_tabela_excel(caminho_temp_precos)[m
[32m+[m[32m        try:[m
[32m+[m[32m            atualizados, avisos = tabela_precos.importar_tabela_excel(caminho_temp_precos)[m
 [m
         for aviso in avisos:[m
             st.warning(aviso)[m
[36m@@ -173,6 +176,10 @@[m [mwith st.expander("💲 Personalizar Tabela de Preços (avançado)"):[m
         elif not avisos:[m
             st.info("Nenhum preço foi alterado em relação ao que já está em uso.")[m
 [m
[32m+[m[32m        finally:[m
[32m+[m[32m            if os.path.exists(caminho_temp_precos):[m
[32m+[m[32m                os.remove(caminho_temp_precos)[m
[32m+[m
     overrides_atuais = tabela_precos.carregar_overrides()[m
     if overrides_atuais:[m
         st.caption(f"📌 {len(overrides_atuais)} preço(s) customizado(s) em uso atualmente.")[m
[36m@@ -191,7 +198,6 @@[m [mnome_projeto = st.text_input([m
     key="input_nome_projeto"[m
 )[m
 [m
[31m-LOCAL_OBRA = "Boa Vista/RR"[m
 [m
 col_pad, col_est = st.columns(2)[m
 with col_pad:[m
[36m@@ -212,10 +218,9 @@[m [mif arquivo_pdf is not None:[m
     st.success("Planta carregada com sucesso!")[m
 [m
     extensao = os.path.splitext(arquivo_pdf.name)[1][m
[31m-    arquivo_temp = f"temp_planta{extensao}"[m
[31m-[m
[31m-    with open(arquivo_temp, "wb") as f:[m
[31m-        f.write(arquivo_pdf.getbuffer())[m
[32m+[m[32m    with tempfile.NamedTemporaryFile(delete=False, suffix=extensao) as tmp:[m
[32m+[m[32m        tmp.write(arquivo_pdf.getbuffer())[m
[32m+[m[32m        arquivo_temp = tmp.name[m
 [m
     if st.button("🔍 Analisar Planta", key="btn_analisar"):[m
         with st.spinner("Analisando planta com IA..."):[m
[36m@@ -265,21 +270,21 @@[m [mif arquivo_pdf is not None:[m
         with col1:[m
             area_piso_seco = st.number_input([m
                 "Área Seca — m² (sala, quarto, cozinha)",[m
[31m-                value=float(dados.get("area_piso_seco", 0)), step=0.5, min_value=0.0,[m
[32m+[m[32m                value=float(dados.get("area_piso_seco") or 0), step=0.5, min_value=0.0,[m
                 key="input_area_seca"[m
             )[m
             mostrar_badge("area_piso_seco", confianca)[m
         with col2:[m
             area_piso_molhado = st.number_input([m
                 "Área Molhada — m² (banheiro, área de serviço)",[m
[31m-                value=float(dados.get("area_piso_molhado", 0)), step=0.5, min_value=0.0,[m
[32m+[m[32m                value=float(dados.get("area_piso_molhado") or 0), step=0.5, min_value=0.0,[m
                 key="input_area_molhada"[m
             )[m
             mostrar_badge("area_piso_molhado", confianca)[m
         with col3:[m
             area_piso_externo = st.number_input([m
                 "Área Externa — m² (varanda, garagem)",[m
[31m-                value=float(dados.get("area_piso_externo", 0)), step=0.5, min_value=0.0,[m
[32m+[m[32m                value=float(dados.get("area_piso_externo") or 0), step=0.5, min_value=0.0,[m
                 key="input_area_externa"[m
             )[m
             mostrar_badge("area_piso_externo", confianca)[m
[36m@@ -289,28 +294,28 @@[m [mif arquivo_pdf is not None:[m
         with col4:[m
             metros_parede = st.number_input([m
                 "Paredes Lineares (m)",[m
[31m-                value=float(dados["metros_parede"]), step=0.5, min_value=0.0,[m
[32m+[m[32m                value=float(dados.get("metros_parede") or 0), step=0.5, min_value=0.0,[m
                 key="input_metros_parede"[m
             )[m
             mostrar_badge("metros_parede", confianca)[m
 [m
             portas_internas = st.number_input([m
                 "Portas Internas (un)",[m
[31m-                value=int(dados.get("portas_internas", 0)), step=1, min_value=0,[m
[32m+[m[32m                value=int(dados.get("portas_internas") or 0), step=1, min_value=0,[m
                 key="input_portas_internas"[m
             )[m
             mostrar_badge("portas_internas", confianca)[m
         with col5:[m
             janelas = st.number_input([m
                 "Janelas (un)",[m
[31m-                value=int(dados.get("janelas", 0)), step=1, min_value=0,[m
[32m+[m[32m                value=int(dados.get("janelas") or 0), step=1, min_value=0,[m
                 key="input_janelas"[m
             )[m
             mostrar_badge("janelas", confianca)[m
 [m
             portas_externas = st.number_input([m
                 "Portas Externas (un)",[m
[31m-                value=int(dados.get("portas_externas", 0)), step=1, min_value=0,[m
[32m+[m[32m                value=int(dados.get("portas_externas") or 0), step=1, min_value=0,[m
                 key="input_portas_externas"[m
             )[m
             mostrar_badge("portas_externas", confianca)[m
[1mdiff --git a/config.py b/config.py[m
[1mindex 4c12f42..3903ca6 100644[m
[1m--- a/config.py[m
[1m+++ b/config.py[m
[36m@@ -83,3 +83,6 @@[m [mDADOS_MOCK = {[m
         "janelas": {"nivel": "baixa", "motivo": "Planta mock, sem base visual real"},[m
     },[m
 }[m
[32m+[m
[32m+[m[32m# Local padrão da obra (usado em PDF e histórico)[m
[32m+[m[32mLOCAL_OBRA = os.environ.get("LOCAL_OBRA", "Boa Vista/RR")[m
[1mdiff --git a/core/reporter.py b/core/reporter.py[m
[1mindex 08c467a..a6570af 100644[m
[1m--- a/core/reporter.py[m
[1m+++ b/core/reporter.py[m
[36m@@ -78,7 +78,6 @@[m [mdef gerar_excel(dados_orcamento, output_path, bdi_percentual=0):[m
         ws.cell(row=linha_atual, column=1, value=tipo.upper())[m
         linha_atual += 1[m
 [m
[31m-        _primeira_linha_grupo = linha_atual[m
         for i, item in enumerate(itens_grupo):[m
             zebra = cinza_claro if i % 2 == 1 else "FFFFFF"[m
             fill_zebra = PatternFill(start_color=zebra, end_color=zebra, fill_type="solid")[m
[36m@@ -102,7 +101,6 @@[m [mdef gerar_excel(dados_orcamento, output_path, bdi_percentual=0):[m
 [m
             linha_atual += 1[m
 [m
[31m-        _ultima_linha_grupo = linha_atual - 1[m
 [m
         subtotal_valor = round(sum([m
             it["Quantidade"] * it["Preco_Unit"] for it in itens_grupo[m
[1mdiff --git a/core/tabela_precos.py b/core/tabela_precos.py[m
[1mindex 69760b7..69c0a65 100644[m
[1m--- a/core/tabela_precos.py[m
[1m+++ b/core/tabela_precos.py[m
[36m@@ -151,7 +151,7 @@[m [mdef importar_tabela_excel(caminho_arquivo):[m
 def salvar_overrides(precos_atualizados):[m
     """Persiste os precos customizados em disco (precos_customizados.json),[m
     com a data do upload como nova data de referencia desses itens."""[m
[31m-    hoje = datetime.now(tz=timezone.utc).date().strftime("%Y-%m-%d")[m
[32m+[m[32m    hoje = datetime.now(tz=timezone.utc).strftime("%Y-%m")[m
     dados = carregar_overrides()[m
     for chave, valor in precos_atualizados.items():[m
         dados[chave] = {"valor": valor, "data_ref": hoje}[m
[1mdiff --git a/core/vision.py b/core/vision.py[m
[1mindex 6ee699d..bca653b 100644[m
[1m--- a/core/vision.py[m
[1m+++ b/core/vision.py[m
[36m@@ -30,58 +30,7 @@[m [mCAMPOS_AGREGADOS = ([m
     "metros_parede", "portas_internas", "portas_externas", "janelas",[m
 )[m
 [m
[31m-[m
[31m-def _redimensionar_para_max(imagem_cv, max_dim=1024):[m
[31m-    """Reduz a imagem para que o lado maior tenha no máximo max_dim pixels.[m
[31m-    Isso diminui o payload da API, acelera o upload e evita estourar[m
[31m-    limites de tamanho do Gemini sem perder legibilidade de plantas."""[m
[31m-    altura, largura = imagem_cv.shape[:2][m
[31m-    if max(altura, largura) <= max_dim:[m
[31m-        return imagem_cv[m
[31m-    escala = max_dim / max(altura, largura)[m
[31m-    nova_largura = int(largura * escala)[m
[31m-    nova_altura = int(altura * escala)[m
[31m-    return cv2.resize(imagem_cv, (nova_largura, nova_altura), interpolation=cv2.INTER_AREA)[m
[31m-[m
[31m-[m
[31m-def _preparar_imagem(caminho_arquivo):[m
[31m-    """[m
[31m-    Sempre devolve uma imagem JPEG otimizada para enviar ao Gemini.[m
[31m-    PDFs e imagens passam pelo mesmo pré-processamento.[m
[31m-    """[m
[31m-[m
[31m-    if caminho_arquivo.lower().endswith(".pdf"):[m
[31m-        import fitz[m
[31m-[m
[31m-        with fitz.open(caminho_arquivo) as doc:[m
[31m-            pagina = doc[0][m
[31m-            pix = pagina.get_pixmap(dpi=200)[m
[31m-[m
[31m-        with tempfile.NamedTemporaryFile([m
[31m-            suffix=".jpg",[m
[31m-            delete=False[m
[31m-        ) as temp:[m
[31m-            imagem_temporaria = temp.name[m
[31m-[m
[31m-        try:[m
[31m-            pix.save(imagem_temporaria)[m
[31m-            imagem = melhorar_imagem(imagem_temporaria)[m
[31m-        finally:[m
[31m-            if os.path.exists(imagem_temporaria):[m
[31m-                os.remove(imagem_temporaria)[m
[31m-[m
[31m-    else:[m
[31m-        imagem = melhorar_imagem(caminho_arquivo)[m
[31m-[m
[31m-    # Reduz dimensão e comprime para diminuir payload da API[m
[31m-    imagem = _redimensionar_para_max(imagem, max_dim=1024)[m
[31m-    _, buffer = cv2.imencode(".jpg", imagem, [int(cv2.IMWRITE_JPEG_QUALITY), 85])[m
[31m-[m
[31m-    return buffer.tobytes()[m
[31m-[m
[31m-[m
[31m-def _montar_prompt():[m
[31m-    return """[m
[32m+[m[32mPROMPT_EXTRACAO = """[m
     Analise esta imagem de planta baixa de engenharia/arquitetura.[m
 [m
     Antes de responder, raciocine internamente assim, COMODO POR[m
[36m@@ -200,6 +149,62 @@[m [mdef _montar_prompt():[m
     }[m
     """[m
 [m
[32m+[m[32mdef _montar_prompt():[m
[32m+[m[32m    return PROMPT_EXTRACAO[m
[32m+[m
[32m+[m
[32m+[m
[32m+[m[32mdef _redimensionar_para_max(imagem_cv, max_dim=1024):[m
[32m+[m[32m    """Reduz a imagem para que o lado maior tenha no máximo max_dim pixels.[m
[32m+[m[32m    Isso diminui o payload da API, acelera o upload e evita estourar[m
[32m+[m[32m    limites de tamanho do Gemini sem perder legibilidade de plantas."""[m
[32m+[m[32m    altura, largura = imagem_cv.shape[:2][m
[32m+[m[32m    if max(altura, largura) <= max_dim:[m
[32m+[m[32m        return imagem_cv[m
[32m+[m[32m    escala = max_dim / max(altura, largura)[m
[32m+[m[32m    nova_largura = int(largura * escala)[m
[32m+[m[32m    nova_altura = int(altura * escala)[m
[32m+[m[32m    return cv2.resize(imagem_cv, (nova_largura, nova_altura), interpolation=cv2.INTER_AREA)[m
[32m+[m
[32m+[m
[32m+[m[32mdef _preparar_imagem(caminho_arquivo):[m
[32m+[m[32m    """[m
[32m+[m[32m    Sempre devolve uma imagem JPEG otimizada para enviar ao Gemini.[m
[32m+[m[32m    PDFs e imagens passam pelo mesmo pré-processamento.[m
[32m+[m[32m    """[m
[32m+[m
[32m+[m[32m    if caminho_arquivo.lower().endswith(".pdf"):[m
[32m+[m[32m        import fitz[m
[32m+[m
[32m+[m[32m        with fitz.open(caminho_arquivo) as doc:[m
[32m+[m[32m            pagina = doc[0][m
[32m+[m[32m            pix = pagina.get_pixmap(dpi=200)[m
[32m+[m
[32m+[m[32m        with tempfile.NamedTemporaryFile([m
[32m+[m[32m            suffix=".jpg",[m
[32m+[m[32m            delete=False[m
[32m+[m[32m        ) as temp:[m
[32m+[m[32m            imagem_temporaria = temp.name[m
[32m+[m
[32m+[m[32m        try:[m
[32m+[m[32m            pix.save(imagem_temporaria)[m
[32m+[m[32m            imagem = melhorar_imagem(imagem_temporaria)[m
[32m+[m[32m        finally:[m
[32m+[m[32m            if os.path.exists(imagem_temporaria):[m
[32m+[m[32m                os.remove(imagem_temporaria)[m
[32m+[m
[32m+[m[32m    else:[m
[32m+[m[32m        imagem = melhorar_imagem(caminho_arquivo)[m
[32m+[m
[32m+[m[32m    # Reduz dimensão e comprime para diminuir payload da API[m
[32m+[m[32m    imagem = _redimensionar_para_max(imagem, max_dim=1024)[m
[32m+[m[32m    _, buffer = cv2.imencode(".jpg", imagem, [int(cv2.IMWRITE_JPEG_QUALITY), 85])[m
[32m+[m
[32m+[m[32m    return buffer.tobytes()[m
[32m+[m
[32m+[m
[32m+[m
[32m+[m
 [m
 def _chamar_gemini_com_uma_chave(chave, prompt, img_base64):[m
     """Faz a chamada a API com UMA chave especifica, com ate 3[m
[36m@@ -321,17 +326,21 @@[m [mdef extrair_dados_da_planta(caminho_arquivo):[m
             detalhe_tecnico=f"JSONDecodeError: {e}. Texto bruto: {texto_resposta[:500]}",[m
         )[m
 [m
[31m-    # Defesa: garante que todos os 7 campos agregados existam, mesmo se[m
[31m-    # o modelo esquecer algum (preenche com 0).[m
[32m+[m[32m    # Defesa: garante que todos os 7 campos agregados existam e nunca[m
[32m+[m[32m    # sejam None (a Gemini as vezes retorna null em vez de omitir).[m
[32m+[m[32m    for campo in CAMPOS_AGREGADOS:[m
[32m+[m[32m        if dados.get(campo) is None:[m
[32m+[m[32m            dados[campo] = 0[m
[32m+[m
     # ------------------------------------------------------------------[m
     # FALLBACK: corrige metros de parede se a IA subestimou[m
     # ------------------------------------------------------------------[m
     area_total = ([m
[31m-        dados.get("area_piso_seco", 0)[m
[31m-        + dados.get("area_piso_molhado", 0)[m
[31m-        + dados.get("area_piso_externo", 0)[m
[32m+[m[32m        (dados.get("area_piso_seco") or 0)[m
[32m+[m[32m        + (dados.get("area_piso_molhado") or 0)[m
[32m+[m[32m        + (dados.get("area_piso_externo") or 0)[m
     )[m
[31m-    mp = dados.get("metros_parede", 0)[m
[32m+[m[32m    mp = dados.get("metros_parede") or 0[m
     if area_total > 0 and mp < area_total * 0.55:[m
         sugestao = round(area_total * 0.75, 2)[m
         dados["metros_parede"] = sugestao[m
[36m@@ -344,7 +353,8 @@[m [mdef extrair_dados_da_planta(caminho_arquivo):[m
     # ------------------------------------------------------------------[m
 [m
     for campo in CAMPOS_AGREGADOS:[m
[31m-        dados.setdefault(campo, 0)[m
[32m+[m[32m        if campo not in dados or dados[campo] is None:[m
[32m+[m[32m            dados[campo] = 0[m
 [m
     if "confianca" not in dados:[m
         dados["confianca"] = {}[m
[1mdiff --git a/docker-compose.yml b/docker-compose.yml[m
[1mindex 2fdbe67..0ae5af4 100644[m
[1m--- a/docker-compose.yml[m
[1m+++ b/docker-compose.yml[m
[36m@@ -6,10 +6,12 @@[m [mservices:[m
       - "8501:8501"[m
     volumes:[m
       # Dados persistentes fora do container[m
[32m+[m[32m      # NOTA: arquivos soltos (historico.db, precos_customizados.json)[m
[32m+[m[32m      # NAO devem ser montados individualmente — se nao existirem no host,[m
[32m+[m[32m      # o Docker cria PASTAS com esses nomes e o SQLite quebra.[m
[32m+[m[32m      # Use um diretório data/ no host se precisar persistir esses arquivos.[m
       - ./orcamentos_salvos:/app/orcamentos_salvos[m
       - ./perfil_empresa:/app/perfil_empresa[m
[31m-      - ./historico.db:/app/historico.db[m
[31m-      - ./precos_customizados.json:/app/precos_customizados.json[m
       - ./.cache_ia:/app/.cache_ia[m
     environment:[m
       - GEMINI_API_KEY=${GEMINI_API_KEY:-}[m
[1mdiff --git a/requirements.txt b/requirements.txt[m
[1mindex 32d95dd..63908f6 100644[m
[1m--- a/requirements.txt[m
[1m+++ b/requirements.txt[m
[36m@@ -5,3 +5,4 @@[m [mPyMuPDF[m
 openpyxl[m
 reportlab[m
 opencv-python-headless==4.10.0.84[m
[32m+[m[32mPillow[m
[1mdiff --git a/tests/test_calculator.py b/tests/test_calculator.py[m
[1mindex 7daf6ea..fef57a6 100644[m
[1m--- a/tests/test_calculator.py[m
[1m+++ b/tests/test_calculator.py[m
[36m@@ -16,6 +16,7 @@[m [mfrom core.coeficientes import ([m
     MARGEM_PERDA,[m
     PRECO_BLOCO_CERAMICO,[m
 )[m
[32m+[m[32mfrom core import tabela_precos[m
 from core.models import DadosExtracao[m
 [m
 [m
[36m@@ -28,6 +29,14 @@[m [mdef _item(itens, nome):[m
     raise KeyError(f"item {nome!r} nao encontrado nos itens gerados")[m
 [m
 [m
[32m+[m
[32m+[m[32m@pytest.fixture(autouse=True)[m
[32m+[m[32mdef sem_overrides():[m
[32m+[m[32m    """Garante que os testes rodem com precos padrao."""[m
[32m+[m[32m    tabela_precos.restaurar_padroes()[m
[32m+[m[32m    yield[m
[32m+[m[32m    tabela_precos.restaurar_padroes()[m
[32m+[m
 class TestCalcularMateriaisContaFechada:[m
     def test_bloco_ceramico_bate_com_conta_manual(self):[m
         # 10m lineares de parede x pe direito 2.8m = 28 m2 de parede[m
